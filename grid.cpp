#include <iostream>
#include <tile.hpp>
#include <grid.hpp>

grid::grid(int numTile){
    this->numTile = numTile;
    // We set 13 tiles manually
    for (int i = 0; i < 13; i++)
    {
        for (int j = 1; j < 4; j++)
        {
            // Include rotated images
        }
    }
}